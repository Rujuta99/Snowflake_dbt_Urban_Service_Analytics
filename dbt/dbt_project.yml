name: 'chi_311'
version: '1.0.0'
config-version: 2
profile: 'chi_311'

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  chi_311:
    staging:
      +materialized: view
    marts:
      +materialized: table
